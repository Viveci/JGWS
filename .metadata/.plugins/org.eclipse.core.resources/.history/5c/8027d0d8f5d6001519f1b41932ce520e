package doors;
public class CarClosing extends CarState {
    
    public void click(Car door) {
        door.setState(OPENING);
    }
    
    public void complete(Car door) {
        door.setState(door.CLOSED);
    }
}